# 会员档案模块 — 业务学习成果

> 作者：hehaifeng  
> 时间：2026年7月  
> 主线需求：需求-7165 **儿童健康管理资料采集**

---

## 一、这个模块解决什么业务问题？

### 1.1 一句话概括

会员档案模块让母婴零售企业能够**灵活定义需要收集的会员/宝宝资料字段，多渠道采集数据，并将档案数据与会员主表、宝宝表实时同步**。

### 1.2 典型业务场景

| 场景 | 谁 | 在哪 | 做什么 |
|------|-----|------|--------|
| **小程序注册采集** | 会员（宝妈） | 小程序商城 | 填写采集表，录入自己和宝宝的基本信息、喂养方式等 |
| **导购完善资料** | 导购 | 客员通APP | 面对面帮会员完善更多资料（如过敏史、辅食偏好等） |
| **企微侧边栏** | 导购 | 企业微信 | 在与会员沟通时，侧边栏直接查看/编辑会员档案 |
| **后台360视图** | 总部运营 | ERP后台 | 查看会员完整档案画像，含所有资料项的历史填写记录 |
| **后台档案模板配置** | 总部管理员 | ERP后台 | 新建/修改档案模板，决定要收集哪些资料、哪些必填 |
| **SCRM营销** | 营销系统 | SCRM | 根据档案中的复购属性（奶粉段位、纸尿裤尺码）做精准营销 |

### 1.3 为什么需要这个模块？

在母婴零售行业，会员（宝妈）的消费行为与宝宝的成长阶段紧密相关。企业需要知道：
- 会员的基本信息（姓名、手机号、生日、地址）
- 会员有几个宝宝、每个宝宝的姓名/性别/出生日期
- 每个宝宝的喂养方式（母乳/混合/奶粉）
- 会员的复购偏好（奶粉段位、纸尿裤尺码）

这些信息直接决定了：
- **精准营销**：给喝一段奶粉的宝妈推二段奶粉的优惠券
- **导购服务**：导购知道宝宝月龄，推荐合适的商品
- **数据分析**：按宝宝年龄段分析消费特征

---

## 二、核心业务概念

### 2.1 业务对象关系

```
会员（宝妈）
  ├── 会员自己的档案资料（姓名、手机、生日、地址、过敏史...）
  ├── 宝宝1
  │     └── 宝宝1的档案资料（姓名、性别、出生日期、喂养方式...）
  ├── 宝宝2
  │     └── 宝宝2的档案资料（姓名、性别、出生日期、喂养方式...）
  └── 复购属性（一段奶粉品牌、纸尿裤尺码...）
```

### 2.2 业务术语对照

| 业务术语 | 技术对应 | 说明 |
|---------|---------|------|
| 档案模板 | ms_archives_template | 一份"调查问卷"的定义，如"儿童健康档案" |
| 分类 | ms_archives_template_kind | 问卷内的分组，如"基本信息"、"健康信息" |
| 资料项目 | ms_archives_info | 问卷内的一个字段，如"喂养方式" |
| 子级资料项 | ms_archives_info_child | 系统预置的宝宝子字段，如宝宝信息下的"宝宝姓名"、"宝宝性别" |
| 适用对象 | applicableTarget | 这个资料项是给会员填还是给宝宝填 |
| 系统预置 | entityType="sys" | 系统内置字段（如会员手机号、宝宝出生日期），直接存业务表 |
| 自定义字段 | entityType="self" | 客户自己加的字段（如"过敏源"、"辅食偏好"），存实例表 |
| 实例 | ms_archives_instance | 会员/宝宝对某个资料项填的具体值 |
| 变更日志 | ms_archives_instance_log | 每次修改的留痕记录 |

### 2.3 系统预置 vs 自定义

这是理解整个模块的关键区分：

| 对比维度 | 系统预置（sys） | 自定义（self） |
|---------|----------------|---------------|
| 谁创建的 | 系统出厂自带 | 客户在后台自己加 |
| 能否删除 | ❌ 不能 | ✅ 可以 |
| 数据存哪里 | 直接存会员表/宝宝表 | 存档案实例表 |
| 举例 | 手机号、宝宝姓名、宝宝性别 | 过敏源、辅食偏好、睡眠习惯 |
| 典型 colName | mobile, babyName, babySex | 无 colName，靠 infoId 标识 |

**为什么要区分？** 系统预置字段（如手机号）本身就是会员主表的字段，如果同时存在实例表和会员表，数据冗余且容易出现不一致。自定义字段没有对应的业务表字段，只能存在实例表。

---

## 三、端到端业务流程

### 3.1 流程一：后台配置档案模板

**角色**：总部管理员  
**入口**：ERP后台 → 会员管理 → 档案模板

```
┌─────────────────────────────────────────────────────────┐
│ 步骤1：创建档案模板                                        │
│   输入：模板名称（如"2026儿童健康档案"）                      │
├─────────────────────────────────────────────────────────┤
│ 步骤2：添加分类（分组）                                      │
│   如：基本信息 | 健康信息 | 喂养信息 | 过敏信息                │
│   校验：分类名称不可重复                                     │
├─────────────────────────────────────────────────────────┤
│ 步骤3：在每个分类下添加资料项目                                │
│   对每个资料项配置：                                         │
│   - 资料项名称（如"喂养方式"）                                │
│   - 格式类型（文本/单选/多选/数字/日期/地区）                   │
│   - 适用对象（会员 or 宝宝）                                 │
│   - 是否必填                                                │
│   - 可选值（如"母乳,混合喂养,人工喂养"）                       │
│   - 正则校验（如手机号格式）                                   │
│   校验：同一模板内资料项不可重复                               │
├─────────────────────────────────────────────────────────┤
│ 步骤4：保存或发布                                           │
│   - 保存草稿：模板未生效，小程序不可见                          │
│   - 提交发布：模板生效，小程序采集页可见                        │
│   前置校验：                                                 │
│   - 模板必须包含至少一个「手机号」资料项                         │
│   - 如果分类中已有系统预置的「宝宝信息」，就不能再添加适用对象       │
│     为"宝宝"的其他资料项（功能重复）                            │
└─────────────────────────────────────────────────────────┘
```

**业务规则**：
- 系统预置的模板和资料项不能删除、不能修改（entityType="sys"）
- 模板一旦被会员使用过，资料项就不能随便删（删除时校验是否有关联数据）

### 3.2 流程二：会员在小程序填写档案（新接口 submitAllArchives）

**角色**：会员（宝妈）  
**入口**：小程序商城 → 会员中心 → 完善资料

```
┌─────────────────────────────────────────────────────────────┐
│ 前端（小程序）                                               │
│                                                             │
│ 展示一个采集表单：                                            │
│ ┌─────────────────────────────────┐                         │
│ │ 基本信息（会员）                  │                         │
│ │   姓名：[______________]         │                         │
│ │   手机号：[______________]       │                         │
│ │   生日：[____年__月__日]          │                         │
│ │                                 │                         │
│ │ 宝宝信息（宝宝1）                 │                         │
│ │   宝宝姓名：[______________]      │                         │
│ │   宝宝性别：○男 ○女               │                         │
│ │   出生日期：[____年__月__日]       │                         │
│ │   喂养方式：○母乳 ○混合 ○奶粉      │                         │
│ │                                 │                         │
│ │ [+ 添加宝宝]                     │                         │
│ │                                 │                         │
│ │ 健康信息（会员）                  │                         │
│ │   过敏源：[______________]       │                         │
│ │   辅食偏好：☐米粉 ☐果泥 ☐肉泥     │                         │
│ │                                 │                         │
│ │          [ 提交 ]               │                         │
│ └─────────────────────────────────┘                         │
│                                                             │
│ 提交的数据结构（ArchivesSubmitDTO）：                          │
│ {                                                           │
│   "type": "member",          // 提交主体：会员自己             │
│   "id": "会员ID",                                            │
│   "channel": "PTMINI",       // 渠道：小程序                  │
│   "infoList": [                                              │
│     { "infoId":"资料项1", "itemName":"姓名",                    │
│       "trueValue":"张三", "type":"text" },                   │
│     { "infoId":"资料项2", "itemName":"过敏源",                  │
│       "trueValue":"花生,牛奶", "type":"checkbox" },          │
│     ...                                                     │
│   ]                                                         │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 后端处理（MsArchivesTemplateService.submitAllArchives）        │
│                                                             │
│ 第一步：确定归属                                               │
│   如果 type="member" → 数据归属会员（attrType="ms"）            │
│   如果 type="baby"   → 数据归属宝宝（attrType="baby"）         │
│                       需查出宝宝属于哪个会员                     │
│                                                             │
│ 第二步：批量查资料项定义                                        │
│   查 ms_archives_info，知道每个资料项的 colName、entityType      │
│   查 ms_archives_info_child，知道宝宝子字段的定义                │
│                                                             │
│ 第三步：分层处理数据                                            │
│   ┌──────────────────────────────────────┐                   │
│   │ 系统预置（sys）的数据                   │                   │
│   │ → 跳过实例表                           │                   │
│   │ → 直接同步到会员表/宝宝表                │                   │
│   │   例：手机号 → ms_member_def.mobile    │                   │
│   │       宝宝姓名 → ms_member_baby.babyName│                  │
│   └──────────────────────────────────────┘                   │
│   ┌──────────────────────────────────────┐                   │
│   │ 自定义（self）的数据                    │                   │
│   │ → 更新 ms_archives_real（模板实例快照）  │                   │
│   │ → 先删 ms_archives_instance 旧数据     │                   │
│   │ → 再插 ms_archives_instance 新数据     │                   │
│   │ → 多选值拆分：                          │                   │
│   │     "花生,牛奶" → 两条记录，分别存"花生"、"牛奶"│              │
│   │ → 对比新旧值，写 ms_archives_instance_log │                 │
│   └──────────────────────────────────────┘                   │
│                                                             │
│ 第四步：同步业务表                                              │
│   会员维度 → ms_member_def（手机号唯一性校验）                    │
│   宝宝维度 → ms_member_baby（babyName/babySex/babyBirth...）  │
│   复购维度 → ms_repurchase_attributes（奶粉段位/纸尿裤尺码）      │
│                                                             │
│ 第五步：记录变更日志                                            │
│   ms_archives_instance_log：                                  │
│   - 记录操作人（submitUserId/Name）                            │
│   - 记录操作渠道（PTMINI/APP/ERP...）                          │
│   - 记录变更前后的值（oldValue → newValue）                     │
│   - 记录操作类型（INSERT 首次填写 / UPDATE 值变更 / DELETE 值清空）│
└─────────────────────────────────────────────────────────────┘
```

**关键业务规则**：
- 手机号必须唯一，如果其他会员已绑定该手机号则提交失败
- 提交人是会员自己时，submitUserId = 会员ID；导购代填时，submitUserId = 导购工号
- 多选值（如爱好"足球,篮球,乒乓球"）拆成多条记录存储，方便后续按单个值搜索

### 3.3 流程三：导购在客员通/企微侧边栏帮会员填档案

**角色**：导购  
**入口**：客员通APP / 企业微信侧边栏

流程同上（3.2），区别在于：
- `channel` 不同（GuestPass / WeChatSidebar）
- `submitUserId` = 导购的工号（而非会员ID）
- 变更日志会记录是哪个导购操作的，方便追溯

### 3.4 流程四：ERP后台查看会员档案（360视图）

**角色**：总部运营  
**入口**：ERP后台 → 会员管理 → 会员详情 → 360视图 → 档案资料

```
后台请求 → getFinshedTemplate(templateId, memberId, channel, babyId)
         │
         ├─ 查模板定义（有哪些分类、哪些资料项）
         ├─ 查 ms_archives_real（取会员/宝宝已填的值）
         ├─ LEFT JOIN ms_archives_instance（取自定义字段已填的值）
         ├─ 宝宝系统预置字段：从 ms_archives_info_child 平铺为一级资料项
         ├─ 组装返回：把填好的值填入对应的资料项
         └─ 返回完整模板结构（含分类、资料项和已填值）
```

**业务场景**：运营人员查看某会员的完整档案画像，包括该会员自己的资料和他所有宝宝的资料。切换不同宝宝时，展示对应宝宝的资料。

### 3.5 流程五：老接口兼容（客情72记 + 小程序）

**调用点**：客情72记、小程序（历史版本）

这是一个更"重"的接口 `saveReal()`，与 `submitAllArchives()` 的区别：

| 对比 | saveReal（老） | submitAllArchives（新） |
|------|-------------|---------------------|
| 入参方式 | 按模板提交整个 infoMap | 按资料项列表提交 infoList |
| 差异识别 | 遍历所有资料项，逐个判断"新增/修改/删除" | 前端传什么就存什么 |
| 适用场景 | 按模板整体提交（如客情72记） | 增量提交（如小程序逐项填写） |
| 宝宝维度 | 通过入参的 babyId 字段区分 | 通过 type 字段区分 |

---

## 四、数据流向全景图

```
                         ┌──────────────┐
                         │  小程序商城    │
                         │  (PTMINI)    │
                         └──────┬───────┘
                                │ 提交档案数据
                                ▼
┌──────────┐     ┌─────────────────────────────┐     ┌──────────────┐
│ 客员通APP │────►│                             │      │ 企微侧边栏    │
│(GuestPass)│     │   submitAllArchives()      │◄────│(WeChatSidebar)│
└──────────┘     │         或                   │     └──────────────┘
                 │   saveReal()               │
                 │                             │
┌──────────┐     │  ┌───────────────────────┐  │     ┌──────────────┐
│ ERP后台   │────►│  │ 系统预置 → 业务表       │  │     │  SCRM系统    │
│  (ERP)    │     │  │ 自定义   → 实例表+日志表 │  │◄────│  (读档案数据) │
└──────────┘     │  └───────────────────────┘  │     └──────────────┘
                 └─────────────┬───────────────┘
                               │ 同步
                 ┌─────────────┼───────────────┐
                 ▼             ▼               ▼
          ┌──────────┐  ┌──────────┐  ┌────────────────┐
          │会员主表   │  │ 宝宝表    │  │ 复购属性表       │
          │ms_member_│  │ms_member_│  │ms_repurchase_  │
          │def       │  │baby      │  │attributes      │
          └──────────┘  └──────────┘  └────────────────┘
```

---

## 五、关键业务规则总结

### 5.1 数据归属规则

```
适用对象 = "会员" → 数据挂在会员下面（attrType = "ms"）
适用对象 = "宝宝" → 数据挂在宝宝下面（attrType = "baby"）
                       但宝宝本身属于某个会员（通过 memberId 关联）
```

### 5.2 系统预置数据保护

| 操作 | 系统预置 | 自定义 |
|------|---------|--------|
| 修改资料项定义 | ❌ | ✅ |
| 删除资料项 | ❌ | ✅（未使用时） |
| 停用模板 | ❌ | ✅ |
| 在系统预置分类下加子分类 | ❌ | - |

### 5.3 模板发布的前置校验

1. 模板名称不能为空
2. 至少有一个分类
3. 每个分类下至少有一个资料项
4. 同一个模板内资料项名称不重复
5. 同一个分类名称不重复
6. **必须包含至少一个"手机号"资料项**（用于标识会员）
7. **适用对象为"宝宝"的资料项，不能与系统预置的"宝宝信息"资料项共存**（功能重复）

### 5.4 多渠道写入规则

| 渠道 | 标识 | 谁操作 | 提交人ID取值 |
|------|------|-------|-------------|
| 小程序 | PTMINI | 会员自己 | 会员ID |
| 掌上运营 | APP | 导购 | 导购工号 |
| 客员通 | GuestPass | 导购 | 导购工号 |
| 企微侧边栏 | WeChatSidebar | 导购 | 导购工号 |
| SCRM | SCRM | 系统 | 系统标识 |
| ERP后台 | ERP | 后台用户 | 后台用户ID |

当提交人ID为空时，兜底取值为会员自己的ID（小程序免登录场景）。

---

## 六、本月新增业务能力（需求-7165）

### 6.1 宝宝档案独立管理

**改造前**：档案模块只能管理会员维度的资料，宝宝信息作为会员资料项的一个子集存在，无法独立管理。

**改造后**：
- 宝宝有独立的档案资料（attrType="baby"）
- 每个宝宝的资料可以独立增删改查
- 切换宝宝时，展示该宝宝独有的资料项和值
- 宝宝的资料项同步到 `ms_member_baby` 表

### 6.2 档案变更可追溯

**改造前**：修改档案后无法知道"谁、在什么时候、通过哪个渠道、把什么值改成了什么"。

**改造后**：
- 新增 `ms_archives_instance_log` 表
- 每次提交档案时，自动对比新旧值，生成变更日志
- 日志包含：操作人、操作时间、渠道、变更前后的值、操作类型（新增/修改/删除）
- 多选值的日志使用 `showTruedValue`（完整拼接值）而非 `trueValue`（拆分后的单值），避免日志记录不准确

### 6.3 宝宝信息与自定义资料项互斥

**业务背景**：系统预置的"宝宝信息"资料项已经包含了宝宝姓名、性别、出生日期、喂养方式等字段。如果再添加一个适用对象为"宝宝"的自定义资料项，会导致功能重复和界面混乱。

**改造后**：模板保存时自动校验——如果当前分类中已有系统预置的"宝宝信息"，则不允许再添加适用对象为"宝宝"的资料项。

---

## 七、个人业务理解总结

### 7.1 这个模块的本质

会员档案模块本质上是一个**可配置的动态表单系统**：
- **模板** = 表单定义（有哪些字段）
- **实例** = 表单填写记录（具体填了什么）
- **同步** = 表单数据回写到业务表（会员表、宝宝表）

### 7.2 最核心的业务决策

**为什么系统预置字段和自定义字段走不同的存储路径？**

这是整个模块最关键的设计决策。系统预置字段（手机号、宝宝姓名等）本身就是会员表和宝宝表的字段，如果同时在实例表中存一份，会导致：
1. 数据冗余
2. 数据不一致风险（实例表改了但会员表没改）
3. 查询复杂度增加（不知道以哪个为准）

所以系统预置字段跳过实例表，直接更新业务表；自定义字段因为没有对应的业务表字段，才存入实例表。

### 7.3 本月最大的收获

1. **理解了"配置化"的价值**：不同客户需要的资料字段完全不同（有的需要过敏史、有的需要辅食偏好），通过模板配置而非硬编码，实现了灵活的个性化
2. **理解了"多端一致性"的挑战**：同一个档案可以被小程序、客员通、企微侧边栏、ERP后台等多个入口修改，需要保证数据的一致性（通过统一的 Service 入口 + 事务控制）
3. **理解了"变更可追溯"的重要性**：在母婴零售场景中，档案数据直接影响精准营销的效果，必须知道数据是什么时候、被谁修改的
4. **理解了"渐进式改造"的方法**：老接口 saveReal 继续兼容，新接口 submitAllArchives 逐步替代，不是一次性推翻重来
